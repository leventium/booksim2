[project]
name = "bswrap"
version = "0.1.0"
description = "The BookSim wrapper for simultaneous simulation run and data collection"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "tqdm>=4.67.3",
]
